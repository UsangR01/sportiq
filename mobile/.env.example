# Backend base URL. EXPO_PUBLIC_ prefix makes it available in client code via process.env.
# Android emulator can't reach "localhost" (that's the emulator's own loopback) — use
# 10.0.2.2 instead. iOS simulator and web can both use localhost directly.
EXPO_PUBLIC_API_URL=http://localhost:8000
