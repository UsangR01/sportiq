# API framework
fastapi>=0.115
uvicorn[standard]>=0.30
gunicorn>=22.0

# Data layer
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
redis>=5.0

# Task queue
celery[redis]>=5.4
# Provides watchmedo, used by scripts/dev_worker.py to restart the Celery worker on code
# changes. Celery has no --reload of its own (removed in 4.x), so without this a worker keeps
# serving the code it launched with — which silently produced wrong data four separate times
# in one day, each time looking like a broken fix rather than a stale process.
watchdog>=4.0

# Config, auth, validation
pydantic[email]>=2.9
pydantic-settings>=2.4
passlib[argon2]>=1.7
pyjwt>=2.9
httpx>=0.27
slowapi>=0.1.9

# ML
xgboost>=2.1
catboost>=1.2
scikit-learn>=1.5
scipy>=1.14
pandas>=2.2
joblib>=1.4
mlflow>=2.16
optuna>=4.0
pyarrow>=17.0  # parquet caching for ml/training/collect_nba_data.py

# Sport data source SDKs
nba_api>=1.6      # offline historical training only — never called from production workers
balldontlie>=0.1  # NBA live production stats
requests>=2.32    # RotoWire calls (synchronous, run inside Celery worker)

# Push notifications
exponent-server-sdk>=2.1

# Observability (TDD §11)
sentry-sdk[fastapi,celery]>=2.14

# Dev / test / lint
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.6
black>=24.8
